{
  "name": "capitaine",
  "version": "0.1.0",
  "description": "A git-native repo-agent vessel — the repo IS the agent",
  "main": "src/worker.ts",
  "scripts": {
    "start": "wrangler dev",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "bash tests/run-all.sh"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "tsx": "^4.0.0",
    "wrangler": "^3.0.0"
  },
  "keywords": ["git", "agent", "vessel", "capitaine", "cloudflare-workers"],
  "author": "SuperInstance & Lucineer",
  "license": "MIT"
}
