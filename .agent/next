Assess Issue #1: Critical UI Improvements – determine if immediate patch is required or can be queued.  
Create the file lib/discovery.ts with this exact content: export async function scanOrg(org: string): Promise<string[]> { return ['vessel-1', 'vessel-2']; }